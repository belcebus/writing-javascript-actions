name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout!
        uses: actions/checkout@v2
      - name: Hello world!
        run: |
          echo "hello world 1"
          echo "hello world 2"
        
